RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Quiz Next.js na Vercel. O restante da LP (e do site) continua estático.
RewriteRule ^diagnostico(/.*)?$ quiz-proxy.php [L,QSA]

DirectoryIndex index.html
